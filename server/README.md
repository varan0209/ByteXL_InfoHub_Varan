# InfoHub Server
Express backend for Weather, Currency, and Quotes.
- Weather: Open‑Meteo Geocoding + Weather (no key)
- Currency: exchangerate.host (no key)
- Quotes: quotable.io (no key) with local fallback

## Run locally
```bash
cd server
npm install
npm run dev
```
Server starts on http://localhost:5000
